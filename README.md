# final_yr_proj - Startup Platform

This repo contains a multi-tenant startup sandbox platform:
- Each startup runs in an isolated sandbox (web + postgres + n8n)
- Platform dashboard manages startups, approvals, deployments
- Ollama agent builds and updates startup websites using Prisma + Postgres
